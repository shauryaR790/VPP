# v++ version roadmap (v0.6 → v1.0)

**Unique promise:** *Same readable `.vpp` file  -  learn instantly with `vpp run` / `vpp repl`, ship natively with `vpp build`. No other language guarantees interpreter/native parity on Python-style syntax with CI proof.*

| Version | Codename | Purpose | Status |
|---------|----------|---------|--------|
| **v0.6** | Interactive | REPL + extension polish | ✅ Shipped |
| **v0.7** | Live | `vpp watch` + `vpp bench` | ✅ Shipped |
| **v0.8** | Insight | Line debugger + VS Code debug | ✅ Shipped |
| **v0.9** | Ecosystem | Test Explorer + registry search | ✅ Shipped |
| **v1.0** | Stable | Frozen SPEC + compat CI | ✅ Shipped |

See [PARITY_PROMISE.md](PARITY_PROMISE.md) for the v1.0 contract.

Each version: compiler bump, CHANGELOG, GitHub tag, Marketplace VSIX.
