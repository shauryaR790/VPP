# 09  -  Pattern Matching

Exhaustive `match` on enums  -  compiler checks all variants are covered.

```powershell
vpp run projects/09-match/main.vpp
```

**Concepts:** exhaustive match, enum patterns
