# 20  -  JSON Config

Parse JSON with `std.json` and model app configuration with structs.

```powershell
vpp run projects/20-json-config/main.vpp
```

**Concepts:** `std.json`, struct config, `vpp.toml` project layout
